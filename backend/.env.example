# ==================== Environment Configuration ====================
# Copy this file to .env and configure your settings

# ==================== Application ====================
DEBUG=false

# ==================== Security ====================
# REQUIRED: Generate a secure random key for production using:
#   openssl rand -hex 32
#   OR: python -c "import secrets; print(secrets.token_hex(32))"
# This key is used for JWT token signing - keep it secret!
# Minimum 32 characters required
# NEVER use default/example values in production!
SECRET_KEY=GENERATE_USING_OPENSSL_RAND_HEX_32_COMMAND_ABOVE

# CORS Origins (comma-separated, REQUIRED in production)
# In production, NEVER use wildcard (*) - specify exact allowed origins
# Each origin must start with http:// or https://
# Example for production: https://app.example.com,https://admin.example.com
# Example for development: http://localhost:5173,http://localhost:5100
CORS_ORIGINS=http://localhost:5173,http://localhost:5100

# HTTPS Configuration (set to true in production)
USE_HTTPS=false
SERVER_DOMAIN=

# ==================== Database ====================
# SQLite (default - single process mode)
DATABASE_URL=sqlite+aiosqlite:///./teamchat.db

# MySQL (recommended for production)
# IMPORTANT: Generate strong passwords using: openssl rand -base64 32
# NEVER use weak passwords like 'password', 'koordinator_db_password', etc.
# DATABASE_URL=mysql+aiomysql://username:STRONG_PASSWORD_HERE@localhost:3306/koordinator

# Connection Pool Settings (for MySQL/PostgreSQL only)
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=1800

# ==================== Redis ====================
# Optional: Required for multi-worker WebSocket support
# Leave empty for single-process in-memory mode
REDIS_URL=

# Redis with auth (recommended for production)
# Generate strong password using: openssl rand -base64 32
# REDIS_URL=redis://:STRONG_PASSWORD_HERE@localhost:6379/0

# ==================== Celery ====================
# Celery uses REDIS_URL as broker if set
# Otherwise falls back to filesystem broker (dev only)

# Start worker: celery -A app.core.celery_app worker --loglevel=info
# Start beat:   celery -A app.core.celery_app beat --loglevel=info

# ==================== Email ====================
# Internal email domain for auto-generated emails
INTERNAL_EMAIL_DOMAIN=example.com
